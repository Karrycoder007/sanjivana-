import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["images.unsplash.com","plus.unsplash.com"], // Add any other external domains here
  },
  /* config options here */
};

export default nextConfig;
